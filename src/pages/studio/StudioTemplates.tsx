/**
 * /studio/templates — Studio template gallery.
 * Reuses the existing TemplatesGallery so Studio shares the same asset catalog
 * as the rest of the platform.
 */
import TemplatesGallery from "@/pages/TemplatesGallery";

const StudioTemplates = () => <TemplatesGallery />;

export default StudioTemplates;
